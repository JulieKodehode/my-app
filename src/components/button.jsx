export default function Button() {
	return (
		// Components needs a parent element. And in most cases we use an empty element like this instead of a div.
		<>
			<button>Trykk på meg!</button>
		</>
	);
}
